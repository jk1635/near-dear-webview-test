import BasicContainer from './BasicContainer.tsx';

const Loading = () => {
    return (
        <BasicContainer>
            <h1>Loading</h1>
        </BasicContainer>
    );
};

export default Loading;
